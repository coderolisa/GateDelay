"use client";
/**
 * Navbar — thin re-export wrapper.
 * The full responsive implementation lives in
 * `components/layout/Navigation.tsx`.
 */
export { default } from "../../components/layout/Navigation";
